spring:
  application:
    name: webapp
  mvc:
    problemdetails:
      enabled: true

bookstore:
  api-gateway-url: http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    git:
      mode: full
      enabled: true

server:
  port: 5000
  shutdown: graceful
