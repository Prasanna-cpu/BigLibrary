spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://orderDB:5432/orderDB}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  rabbitmq:
    host: ${RABBITMQ_HOST:biglibrary-rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

order:
  catalog-service-url: ${ORDER_CATALOG_SERVICE_URL:http://catalog-service:8081}