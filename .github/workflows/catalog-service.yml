name: catalog-service.yml
on:
  push:
    paths:
      - catalog-service/**
    branches:
      - 'main'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      working-directory: ./catalog-service
      DOCKER_IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/catalog-service
    defaults:
      run:
        working-directory: ${{env.working-directory}}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'
      - name: Make Maven Wrapper executable
        run: chmod +x mvnw

      - name: Format code
        run: ./mvnw -ntp spotless:apply

      - name: Build
        run: ./mvnw -ntp verify

      - if: ${{ github.ref == 'refs/heads/main' }}
        name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - if: ${{ github.ref == 'refs/heads/main' }}
        name: Build and Publish Docker Image
        run: |
            ./mvnw spring-boot:build-image -DskipTests
            IMAGE_ID=$(docker images -q ${{ secrets.DOCKERHUB_USERNAME }}/catalog-service)
            docker tag $IMAGE_ID $DOCKER_IMAGE_NAME:latest
            echo "Pushing the image $DOCKER_IMAGE_NAME:latest to Docker Hub..."
            docker push $DOCKER_IMAGE_NAME:latest

